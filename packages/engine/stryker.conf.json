{
  "$schema": "https://raw.githubusercontent.com/stryker-mutator/stryker-js/master/packages/core/schema/stryker-schema.json",
  "_comment": "One-off mutation AUDIT of the incremental-parse engine (not wired into CI — run manually: pnpm --filter @ai-react-markdown/engine exec stryker run). Killer suite = the fast arbiter set, see stryker.vitest.config.ts. Score + surviving-mutant dispositions are recorded in src/experiments/prefixFreeze/README.md.",
  "plugins": ["@stryker-mutator/vitest-runner"],
  "testRunner": "vitest",
  "vitest": {
    "configFile": "stryker.vitest.config.ts"
  },
  "mutate": [
    "src/components/incrementalParse/computeFreezeBoundary.ts",
    "src/components/incrementalParse/spliceParse.ts",
    "src/components/incrementalParse/advanceIncrementalParse.ts"
  ],
  "reporters": ["progress", "clear-text", "html"],
  "htmlReporter": { "fileName": "reports/mutation/index.html" },
  "concurrency": 8,
  "timeoutMS": 30000,
  "disableTypeChecks": true,
  "incremental": false
}
